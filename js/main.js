document.addEventListener('DOMContentLoaded', function() {
    console.log('Документ загружен');

    // Ваш код здесь
    // Например, инициализация карусели:
    const carousel = {
        init: function() {
            console.log('Карусель инициализирована');
        }
    };

    carousel.init();
});